# t-call
T-call — a supervised Linux session gateway that allows administrators to join a user’s live session, approve privileged actions, and maintain strict control without shared shells or unsafe sudo.
